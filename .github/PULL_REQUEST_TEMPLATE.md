## What this changes

<!-- One or two sentences. -->

## The failure mode

<!-- What went wrong that this fixes? A skill exists because something failed twice —
     name it. For a docs or tooling change: what was misleading or missing. -->

## Checks

- [ ] `node tools/check.mjs` — paste the summary line and any new warnings:

```
```

- [ ] Ran against a real repo, not only read <!-- skill changes: which repo, which run -->
- [ ] Shared rules still live only in `agentic-guardrails`
- [ ] Description leads with the situation, not the artefact <!-- new/changed skills -->
- [ ] `## Done when` present <!-- skills with numbered steps -->

## Invocation

<!-- New skill? State whether it is model-invoked or `user-invocable-only`, and why.
     A new skill defaults to hand-invoked; it earns model invocation by being typed
     two or three times in real work. -->

## Anything a reviewer should push back on

<!-- Decisions you made that could reasonably have gone the other way. -->
